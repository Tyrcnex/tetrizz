export const rooms = (get, _, __)=>{
    return async ()=>{
        const res = await get({
            uri: "rooms/"
        });
        if (res === null) return [];
        else {
            if (res.success) return res.rooms;
            else return [];
        }
    };
};

//# sourceMappingURL=rooms.js.map