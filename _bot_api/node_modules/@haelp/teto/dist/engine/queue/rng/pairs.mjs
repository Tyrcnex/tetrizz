import { RNG } from "../../utils/index.mjs";
export const pairs = (seed)=>{
    const gen = new RNG(seed);
    return ()=>{
        const s = gen.shuffleArray([
            "z",
            "l",
            "o",
            "s",
            "i",
            "j",
            "t"
        ]);
        const pairs = gen.shuffleArray([
            s[0],
            s[0],
            s[0],
            s[1],
            s[1],
            s[1]
        ]);
        return pairs;
    };
};

//# sourceMappingURL=pairs.js.map