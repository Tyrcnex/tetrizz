export declare class RNG {
    private static readonly MODULUS;
    private static readonly MULTIPLIER;
    private static readonly MAX_FLOAT;
    private value;
    index: number;
    constructor(seed: number);
    next(): number;
    nextFloat(): number;
    shuffleArray<T extends any[]>(array: T): T;
    get seed(): number;
    set seed(value: number);
    updateFromIndex(index: number): void;
    clone(): RNG;
}
//# sourceMappingURL=index.d.ts.map